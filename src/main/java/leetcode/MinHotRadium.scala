package leetcode

object MinHotRadium {

//  def findRadius(houses: Array[Int], heaters: Array[Int]): Int = {
//    var (min, max) = (houses(0), houses(0))
//    houses.foreach{ house =>
//      min = Math.min(min, house)
//      max = Math.max(max, house)
//    }
//    var maxStep = 0
//
//    val sorted = heaters.sorted
//    for( i <- 0 until sorted.size - 1) {
//      if (heaters(i) < )
//      maxStep = Math.max(sorted(i + 1) - sorted(i), maxStep)
//    }
//    maxStep
//  }
//
//  def main(args: Array[String]): Unit = {
//    val houses = Array(1,2,3,4)
//    val heaters = Array(1,4)
//    println(findRadius(houses, heaters))
//  }

}
